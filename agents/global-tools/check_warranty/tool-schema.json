{
  "name": "check-warranty-status",
  "description": "Check warranty status and coverage for a product. Returns warranty details including expiration date, type, and days remaining.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "Product identifier (e.g., 'laptop-x1', 'monitor-hd27')"
      },
      "user_id": {
        "type": "string",
        "description": "User requesting warranty check (optional, for tracking)"
      }
    },
    "required": ["product_id"]
  }
}
